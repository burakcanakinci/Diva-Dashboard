import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class AuthService {

  // private credentials = { login: 'admin', password: 'admin' };

  // checkCredentials(login: string, password: string): boolean {
  //   return (
  //     login === this.credentials.login && password === this.credentials.password
  //   );
  // }
}